# Checkmate 모노레포 현황 분석 (2026-04-22)

---

## 1. 프로젝트 구조

### 모노레포 형태
별도 관리되는 2개의 독립 저장소 (nx / turborepo / lerna 미사용)

| 패키지 | 역할 | 프레임워크 | 주요 라이브러리 |
|--------|------|----------|----------------|
| `checkmate-backend/` | API 서버 | NestJS 11 + TypeScript | Prisma 6, Supabase, OpenAI, BullMQ, Zod |
| `Checkmate-Frontend/` | 웹 클라이언트 | React 19 + Vite 8 | React Router 7, Axios, Supabase-js, Tailwind 4, @dnd-kit |

---

## 2. 백엔드 구현 현황

### 2-1. API 엔드포인트 (전역 prefix `/api`)

#### Auth
| 메서드 | 경로 | 기능 | 상태 |
|-------|------|------|------|
| GET | /auth/health | 헬스체크 | ✅ |
| GET | /auth/me | 현재 사용자 (JIT 프로비저닝) | ✅ |

#### Users
| 메서드 | 경로 | 기능 | 상태 |
|-------|------|------|------|
| GET | /users/:id | 사용자 조회 | ✅ |
| PATCH | /users/me | 프로필 수정 | ✅ |
| POST | /users/me/consent | 약관 동의 기록 | ✅ |

#### Master (공개, 인증 불필요)
| 메서드 | 경로 | 기능 | 상태 |
|-------|------|------|------|
| GET | /master/countries | 국가 목록 (10개) | ✅ |
| GET | /master/cities | 도시 목록 (필터링) | ✅ |
| GET | /master/checklist-categories | 카테고리 13개 | ✅ |
| GET | /master/travel-styles | 여행스타일 8개 | ✅ |
| GET | /master/companion-types | 동행유형 6개 | ✅ |

#### Trips
| 메서드 | 경로 | 기능 | 상태 |
|-------|------|------|------|
| GET | /trips?userId= | 여행 목록 (최신순) | ✅ |
| GET | /trips/:id | 여행 상세 (관계 전체 포함) | ✅ |
| POST | /trips | 여행 생성 (원자적 트랜잭션) | ✅ |
| PATCH | /trips/:id | 여행 수정 | ✅ |
| DELETE | /trips/:id | 소프트 삭제 | ✅ |

#### Checklists
| 메서드 | 경로 | 기능 | 상태 |
|-------|------|------|------|
| GET | /checklists/by-trip/:tripId | 체크리스트 조회 | ✅ |
| GET | /checklists/by-trip/:tripId/candidates | 후보 풀 조회 | ✅ |
| POST | /checklists/generate/:tripId | 맞춤 생성 (LLM + 템플릿 merge) | ✅ |
| POST | /checklists/generate-from-context | 컨텍스트 기반 생성 | ✅ |
| POST | /checklists/items/:itemId/select | 내 목록 추가 | ✅ |
| POST | /checklists/items/:itemId/deselect | 내 목록 제외 | ✅ |
| POST | /checklists/by-trip/:tripId/items | 아이템 배치 upsert | ✅ |
| PATCH | /checklists/items/:itemId | 아이템 편집 + 로그 | ✅ |
| DELETE | /checklists/items/:itemId | 소프트 삭제 + 로그 | ✅ |
| POST | /checklists/items/:itemId/check | 체크 토글 + 로그 | ✅ |

#### LLM
| 메서드 | 경로 | 기능 | 상태 |
|-------|------|------|------|
| POST | /llm/trips/:tripId/generate | LLM 큐잉 (enqueue만) | ⚠️ 워커 미구현 |
| GET | /llm/trips/:tripId/generations | 생성 이력 | ✅ |

#### Guide Archives
| 메서드 | 경로 | 기능 | 상태 |
|-------|------|------|------|
| GET | /trips/:tripId/guide-archives | 보관함 목록 | ✅ |
| POST | /trips/:tripId/guide-archives | 보관함 생성 | ✅ |
| PATCH | /guide-archives/:archiveId | 보관함 수정 | ✅ |
| DELETE | /guide-archives/:archiveId | 보관함 삭제 | ✅ |

#### Analytics
| 메서드 | 경로 | 기능 | 상태 |
|-------|------|------|------|
| POST | /analytics/events | 이벤트 수집 단건/배치 (202) | ✅ |

---

### 2-2. Prisma 스키마 (PostgreSQL 16)

**마이그레이션 3종**
- `20260420145309_init` — 초기 전체 스키마
- `20260422_add_user_consent_onboarding` — 약관 동의 필드
- `20260422_add_checklist_item_isSelected` — 후보 풀 분리용 필드

**모델 14개**

| 도메인 | 모델 |
|--------|------|
| 사용자 | User, UserAuthProvider, UserPassport |
| 마스터 | Country, City, ChecklistCategory, TravelStyle, CompanionType |
| 여행 | Trip, TripCity, TripFlight, TripCompanion, TripTravelStyle |
| 체크리스트 | Checklist, ChecklistItem, ChecklistItemTemplate, GuideArchive |
| 로그·분석 | ChecklistItemEdit, ChecklistItemCheck, LlmGeneration, UserEvent |

**Enum 14종**: Gender, AuthProvider, BookingStatus, TripStatus, FlightDirection, ChecklistStatus, ChecklistGeneratedBy, PrepType, BaggageType, ChecklistItemSource, EditType, CheckAction, LlmStatus, UserEventType(11종)

---

### 2-3. 인증 / 미들웨어

| 구성 | 상태 | 비고 |
|------|------|------|
| JWT 전역 Guard | ✅ | HS256 → Supabase admin 2단계 폴백 |
| JIT 프로비저닝 | ✅ | 최초 요청 시 users + user_auth_providers 자동 생성 |
| `@Public()` 데코레이터 | ✅ | Master, /auth/health 우회 |
| Helmet (보안 헤더) | ✅ | |
| CORS | ✅ | CORS_ORIGIN 환경변수 쉼표 목록 |
| ValidationPipe (전역) | ✅ | whitelist + transform |
| LoggingInterceptor (전역) | ✅ | |
| HttpExceptionFilter (전역) | ✅ | 표준 에러 응답 |
| **AUTH_DEV_BYPASS** | ⚠️ | **운영 전 반드시 제거** |

---

### 2-4. 구현 완성도

| 구분 | 내용 |
|------|------|
| ✅ 완전 구현 | Trip CRUD(트랜잭션+검증), Checklist 생성(LLM+템플릿 merge), Checklist 아이템 CRUD+로깅, Master, Users, Guide Archive CRUD, Analytics 수집, JWT Guard+JIT |
| ⚠️ 부분 구현 | LLM 비동기 처리 — `src/modules/llm/llm.service.ts:20-21` (BullMQ 워커 TODO) |
| ❌ 미구현 | LLM BullMQ 워커, OKR 집계 쿼리, 테스트 케이스, 크롤러 |

---

## 3. 프론트엔드 구현 현황

### 3-1. 페이지 / 라우팅 (18개 라우트)

| 경로 | 페이지 | 완성도 | 비고 |
|------|-------|:------:|------|
| `/` | HomePage | ✅ 100% | 스크롤 스냅, 피처 3장 |
| `/login` | LoginPage | ✅ 95% | Google/Kakao OAuth |
| `/signup` | SignupPage | ⚠️ 보관 | 페이지 전환 미구현 |
| `/auth/consent` | AuthConsentPage | ✅ 100% | 약관 수집 |
| `/auth/callback` | AuthCallbackPage | ✅ 100% | Supabase 해시 파싱 |
| `/onboarding` | OnboardingProfilePage | ✅ UI만 | `PATCH /users/me` **미연동** |
| `/mypage` | MyPage | ❓ | 세부 조회 필요 |
| `/trips/new/step2` | TripNewStep2Page | ✅ | 예매 여부 선택 |
| `/trips/new/destination` | TripNewDestinationPage | ✅ | 국가/도시/날짜 (mock 사용 중) |
| `/trips/new/step3` | TripNewStep3Page | ✅ | 동행인 선택 (mock 사용 중) |
| `/trips/new/step4` | TripNewStep4Page | ✅ | 여행스타일 (mock 사용 중) |
| `/trips/new/step5` | TripNewStep5Page | ✅ | 항공권 더미 렌더링 |
| `/trips/:id/loading` | TripLoadingPage | ✅ | 풀스크린 진행 애니메이션 |
| `/trips/:id/search` | TripSearchPage | ⚠️ 85% | Store Loop, 전체선택 미구현 |
| `/trips/:id/guide-archive` | TripGuideArchivePage | ✅ | 스냅샷 목록 |
| `/trips/:id/guide-archive/:id` | TripGuideArchiveDetailPage | ⚠️ 90% | DnD 순서 저장 API 미연동 |
| `/404` | NotFoundPage | ✅ | |

---

### 3-2. API 연동 현황

HTTP 클라이언트: axios 1.15, baseURL = `/api` (Vite 프록시 → `localhost:8080`), Supabase 토큰 자동 주입

| 모듈 | 파일 | 상태 |
|------|------|------|
| Auth | `src/api/auth.js` | ✅ 연동 완료 |
| Trips | `src/api/trips.js` | ✅ 연동 완료 |
| Checklists | `src/api/checklists.js` | ⚠️ 일부만 연동 (edit/delete/toggle 스텁 상태, 백엔드는 실제 구현됨) |
| Users | `src/api/users.js` | ⚠️ 함수 있으나 페이지에서 미호출 |
| Master | `src/api/master.js` | ⚠️ 함수 있으나 페이지는 mocks 사용 중 |
| Guide Archives | `src/api/guideArchives.js` | ⚠️ 인메모리로 표기 (백엔드 실제 구현됨, 연동 필요) |
| Analytics | `src/api/analytics.js` | ✅ 연동 완료 |

---

### 3-3. 상태관리

- **외부 상태관리 라이브러리 없음** (Redux/Zustand/Recoil/Context 미사용)
- **서버 상태 관리 없음** (React Query/SWR 미사용)
- **저장소**: `localStorage` 기반 유틸 16종 (`src/utils/`)

---

### 3-4. 더미 데이터 (mocks/) 사용 중인 부분

| 파일 | 사용처 | 교체 대상 |
|------|-------|----------|
| `tripNewDestinationData.js` | 국가·도시 마스터 | `GET /master/countries`, `GET /master/cities` |
| `tripNewStep3Data.js` | 동행인 옵션 | `GET /master/companion-types` |
| `tripNewStep4Data.js` | 여행스타일 옵션 | `GET /master/travel-styles` |
| `tripNewStep5Data.js`, `flightMockData.js` | 항공권 | 백엔드 없음 (별도 구현 필요) |
| `searchData.js` | 준비물 탐색 초기 데이터 | `POST /checklists/generate/:tripId` |
| `homeData.js` | 홈 피처 카드 | 유지 (정적 콘텐츠) |

---

## 4. 프론트-백엔드 연동 교차 분석

### ✅ 연동 완료

| 기능 | 프론트 → 백엔드 |
|------|--------------|
| OAuth 로그인 | Supabase Auth (프론트 전담) |
| 현재 사용자 | `GET /auth/me` |
| 여행 CRUD | `GET|POST|PATCH|DELETE /trips` |
| 체크리스트 생성 | `POST /checklists/generate/:tripId` |
| 체크리스트 후보 조회 | `GET /checklists/by-trip/:tripId/candidates` |
| 이벤트 수집 | `POST /analytics/events` |

### ⚠️ 프론트 코드 있으나 실제 미호출

| 기능 | 이유 |
|------|------|
| 마스터 데이터 API 호출 | 페이지가 mocks에서 읽음 |
| Checklist edit/delete/toggle | 프론트에서 스텁 처리 (백엔드는 있음) |
| Guide Archives CRUD | 인메모리로 처리 (백엔드는 있음) |
| `PATCH /users/me` | OnboardingProfilePage TODO로 남겨둠 |
| `POST /users/me/consent` | AuthConsentPage 연결 확인 필요 |

### ❌ 프론트 호출하나 백엔드 없는 것

| 기능 | 상태 |
|------|------|
| 항공권 검색 (Step5) | 백엔드 없음, mock만 |

### ⚠️ 백엔드 있으나 프론트 미사용

| 엔드포인트 |
|-----------|
| `POST /llm/trips/:tripId/generate` |
| `GET /llm/trips/:tripId/generations` |
| `GET /users/:id` |

---

## 5. 데이터 수집 현황

| 데이터 | 수집 | 저장 | 조회 | 상태 |
|--------|:----:|:----:|:----:|------|
| 여행 정보 | ✅ | ✅ Trip* 테이블 | ✅ | **완성** |
| 체크리스트 (LLM+템플릿) | ✅ | ✅ Checklist* | ✅ | **완성** |
| 체크/편집 로그 | ✅ | ✅ ChecklistItemEdit/Check | ❌ 집계 없음 | **수집만** |
| 사용자 행동 이벤트 | ✅ | ✅ UserEvent | ❌ OKR 쿼리 없음 | **수집만** |
| LLM 비동기 생성 | ✅ enqueue | ✅ pending 레코드 | ❌ 워커 없음 | **미처리** |
| 항공권 외부 | ❌ | ❌ | ❌ | **미구현** |

---

## 6. 다음 작업 우선순위

### 🥇 즉시 — 이미 만들어진 것 연결하기

| # | 작업 | 파일 |
|---|------|------|
| 1 | **Master API 연동** | `TripNewDestinationPage.jsx`, Step3/4 페이지 → `api/master.js` 호출로 교체 |
| 2 | **Checklist 편집/체크 API 연동** | `TripSearchPage.jsx`, `api/checklists.js` 스텁 제거 → 실제 API 호출 |
| 3 | **Guide Archives 실제 DB 연동** | `api/guideArchives.js` 인메모리 → `GET/POST/PATCH/DELETE /trips/:tripId/guide-archives` |
| 4 | **온보딩 프로필 저장** | `OnboardingProfilePage.jsx:82` → `users.updateMyProfile()` 호출 |
| 5 | **약관 동의 API 호출** | `AuthConsentPage.jsx` → `users.acceptLegalConsent()` 호출 확인 |

### 🥈 연동 품질 개선

| # | 작업 |
|---|------|
| 6 | **React Query 도입** — Master/Trip 데이터 서버 상태 관리 (현재 localStorage만) |
| 7 | **localStorage → 서버 동기화** — `savedTripItems`, `guideArchiveStorage` 서버 fetch로 교체 |
| 8 | **AUTH_DEV_BYPASS 제거** — `supabase-jwt.guard.ts` dev-anon 바이패스 운영 전 비활성화 |

### 🥉 데이터 수집 완성

| # | 작업 |
|---|------|
| 9 | **LLM BullMQ 워커 구현** — `src/jobs/llm-generate.processor.ts` 생성 (progress.md NEXT-4) |
| 10 | **OKR 집계 쿼리** — DAU, search→save 전환율, 완료율 p50 집계 엔드포인트 (NEXT-5) |
| 11 | **항공권 검색 백엔드** — 외부 항공 API 통합 |

### 📅 이후

| # | 작업 |
|---|------|
| 12 | Jest/Supertest 테스트 케이스 작성 (설정만 있음) |
| 13 | `20260421fix.md` 12개 섹션 UI 수정 (Step 개수 확정, 카피, 카테고리 시각화 등) |
| 14 | SignupPage 스크롤 → 페이지 전환 방식 변경 |
| 15 | 크롤러/스케줄러 — 도시·항공편·시즌 데이터 자동 수집 |

---

> **핵심 결론**: 백엔드 API는 대부분 완성 상태. 가장 시급한 것은 신규 기능이 아니라 "이미 구현된 양쪽을 실제로 연결하는 작업(1~5번)". 이것만 해도 체감 완성도가 크게 상승함.
